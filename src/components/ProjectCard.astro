---
import { BASE } from '../config';
/*
  Carte projet — grille homepage
  Aucun effet de hover, zoom, ou halo.
  Les tags sont passifs (classe .tag).
*/

interface Props {
  title: string;
  slug: string;
  tags: string[];
  image?: string;
  video?: string;
}

const { title, slug, tags, image, video } = Astro.props;
---

<a href={`${BASE}${slug}`} class="project-card">
  <div class="project-card-media">
    {video ? (
      <video data-src={video} autoplay loop muted playsinline preload="none" class="project-card-visual" />
    ) : image ? (
      <img src={image} alt={`Projet ${title}`} loading="lazy" class="project-card-visual" />
    ) : null}
  </div>

  <div class="project-card-info">
    <h3 class="project-card-title">{title}</h3>
    <div class="project-card-tags">
      {tags.map(tag => <span class="tag">{tag}</span>)}
    </div>
  </div>
</a>

<style>
  .project-card {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  .project-card-media {
    width: 100%;
  }

  .project-card-visual {
    width: 100%;
    height: auto;
    display: block;
  }

  .project-card-info {
    display: flex;
    flex-direction: column;  /* titre au-dessus, tags en-dessous */
    align-items: flex-start;
    gap: 0.45rem;
    padding-top: 0.75rem;
  }

  .project-card-title {
    font-family: var(--font-serif);
    font-size: clamp(1.1rem, 1.8vw, 1.5rem);
    font-weight: normal;
    font-style: normal;
    line-height: 1.25;
  }

  .project-card-tags {
    display: flex;
    gap: 0.35rem;
    flex-wrap: wrap;
  }

  /* Tags lisibles — alignés en taille sur leur hauteur visuelle */
  .project-card-tags .tag {
    font-size: 0.72rem;
    padding: 0.3em 0.65em;
  }
</style>
