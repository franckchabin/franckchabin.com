---
/*
  LAYOUT PROJET
  Structure calquée sur le site Webflow de référence :
  1. Titre (h1, pleine largeur)
  2. Séparateur horizontal
  3. Deux colonnes : gauche (sideInfo) / droite (description + tags)
  4. Galerie — décalée à droite pour s'aligner avec la colonne description
  5. "Projet suivant" — bouton .btn pill en bas à gauche
*/

import Base from './Base.astro';
import { BASE } from '../config';

interface Props {
  title: string;
  description: string;
  tags: string[];
  sideInfo?: string;
  nextProject?: {
    title: string;
    slug: string;
  };
}

const { title, description, tags, sideInfo, nextProject } = Astro.props;
---

<Base title={`${title} — Franck Chabin`} description={description}>
  <article class="project">

    <!-- Titre -->
    <header class="project-header container">
      <h1 class="project-title">{title}</h1>
    </header>

    <!-- Séparateur -->
    <hr class="project-separator container" />

    <!-- Description + infos latérales -->
    <!-- Ordre DOM = ordre mobile : description d'abord, sideInfo ensuite -->
    <!-- Sur desktop, order CSS remet sideInfo à gauche (col 1) -->
    <section class="project-intro container">
      <div class="project-description">
        <p>{description}</p>
        <div class="project-tags">
          {tags.map(tag => (
            <span class="tag">{tag}</span>
          ))}
        </div>
      </div>
      <aside class="project-side-info">
        {sideInfo
          ? <Fragment set:html={sideInfo} />
          : <span></span>
        }
      </aside>
    </section>

    <!-- Galerie -->
    <section class="project-gallery container">
      <slot />
    </section>

    <!-- Navigation projet suivant -->
    {nextProject && (
      <nav class="project-next container" aria-label="Projet suivant">
        <a href={`${BASE}${nextProject.slug}`} class="btn">
          Projet suivant → {nextProject.title}
        </a>
      </nav>
    )}

  </article>
</Base>

<style>
  .project {
    padding-bottom: var(--space-2xl);
  }

  /* --- Titre --- */
  .project-header {
    padding-top: calc(var(--nav-height) + var(--space-xl));
    padding-bottom: var(--space-md);
  }

  .project-title {
    font-family: var(--font-serif);
    font-weight: normal;
    font-size: clamp(3.5rem, 10vw, 8rem);
    line-height: 1;
    letter-spacing: -0.02em;
  }

  /* --- Séparateur --- */
  .project-separator {
    height: 1px;
    background-color: var(--color-border);
    border: none;
    margin: 0;
  }

  /* --- Intro : deux colonnes --- */
  .project-intro {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
    padding-top: var(--space-md);
    padding-bottom: var(--space-lg);
  }

  .project-side-info {
    font-family: var(--font-serif);
    font-size: 1rem;
    line-height: 1.65;
    color: var(--color-text-light);
  }

  .project-description p {
    font-family: var(--font-serif);
    font-size: clamp(1.25rem, 2vw, 1.6rem);
    line-height: 1.5;
    hyphens: none;
    -webkit-hyphens: none;
  }

  .project-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-top: var(--space-md);
  }

  .project-tags .tag {
    font-size: 0.8rem;
  }

  /* --- Galerie --- */
  .project-gallery {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .project-gallery :global(img),
  .project-gallery :global(video) {
    width: 100%;
    height: auto;
    border-radius: var(--radius-card);
    display: block;
  }

  .project-gallery :global(.gallery-bg) {
    padding: var(--space-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-card);
  }

  /* --- Projet suivant --- */
  .project-next {
    margin-top: var(--space-2xl);
  }

  /* =============================================
     DESKTOP : grille 2 colonnes
     ============================================= */
  @media (min-width: 768px) {
    .project-header {
      padding-bottom: var(--space-lg);
    }

    .project-intro {
      grid-template-columns: minmax(0, 1fr) minmax(0, var(--gallery-max));
      gap: var(--space-xl);
      padding-top: var(--space-lg);
    }

    /* Sur desktop, sideInfo revient en colonne gauche (ordre DOM inversé) */
    .project-side-info {
      order: -1;
      overflow: hidden;
    }

    /* Galerie alignée à droite, largeur calée sur 80vh × 1.5 */
    .project-gallery {
      max-width: var(--gallery-max);
      margin-left: auto;
      padding-left: 0;
    }
  }

  @media (min-width: 1024px) {
    .project-header {
      padding-top: calc(var(--nav-height) + var(--space-md));
    }
  }
</style>
